---
interface Props {
	headline: string;
	tagline: string;
}

const { headline, tagline } = Astro.props;
---

<section class="hero-section">
	<h1 class="headline">{headline}</h1>
	<p class="tagline">{tagline}</p>
	<a href="/" class="btn btn-cta">MimaTe</a>
</section>

<style>
	.hero-section {
		min-height: 85vh;
		background-color: var(--clr-tertiary-400);
		background-image: linear-gradient(
				var(--clr-transparent-50),
				var(--clr-transparent-50)
			),
			image-set(
				url(/images/hero-large.avif) type('image/avif'),
				url(/images/hero-large.webp) type('image/webp'),
				url(/images/hero-large.jpg) type('image/jpg')
			);
		background-size: cover;
		background-repeat: no-repeat;
		background-position: center;

		color: var(--clr-tertiary-100);

		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;

		flex: 1;
		padding-block-start: min(8vw, 200px);
	}

	.headline,
	.tagline {
		margin-block-start: 0;
		margin-inline: 0;
		text-align: center;
	}

	.headline {
		font-family: Laisha;
		font-feature-settings: 'salt' 1;
	}

	.headline {
		font-size: var(--fs-phone-h1);
		font-weight: var(--fw-200);
		line-height: 1;
		margin-block-end: 0.23em;
	}

	.tagline {
		font-size: var(--fs-phone-h2);
		font-family: Merienda;
		max-width: 22ch;
		line-height: 1.3;
	}

	:global(a[class]) {
		text-decoration: none;
	}

	:global(.btn) {
		display: block;
		padding: var(--em-spacing-50) var(--em-spacing-125);
		border-radius: var(--rad-25);
		border: 1px solid currentColor;
		cursor: pointer;
		transition: all 250ms;
	}

	.btn-cta {
		font-size: var(--fs-phone-h3);
		color: var(--clr-primary-200);
		background-color: var(--clr-transparent-50);
		letter-spacing: 2px;

		&:hover {
			background-color: var(--bg-color);
			color: var(--text-color);
		}

		&:active {
			opacity: 0.75;
		}
	}
</style>
